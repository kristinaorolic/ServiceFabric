<style>
    h1 {
        font-family: 'Maiandra GD';
        color: snow;
        text-align: center;
        vertical-align: middle;
        font-size: 50px;
    }

    body {
        background-image: url("https://www.ekapija.com/thumbs/alat_140916_tw1024.jpg");
        background-size: auto;
    }

    table {
        background-color: snow;
        margin-left: auto;
        margin-right: auto;
        border-style: solid;
        width: 50%;
    }

    td, tr {
        font-family: 'Maiandra GD';
        font-size: 24px;
        text-align: center;
        vertical-align: middle;
    }

    input {
        height: 30px;
        text-align: center;
        vertical-align: middle;
        font-family: 'Times New Roman';
        font-size: medium;
    }

    h2 {
        font-family: 'Maiandra GD';
        font-size: 30px;
        text-align: left;
        vertical-align: middle;
        color: white;
    }

    input.submit {
        color: #1cc49d;
        background-color: #1b2f31;
        border-radius: 50px;
        justify-content: center;
        align-items: center;
        height: 3em;
        width: 8em;
        font-size: large;
        font-weight: 600;
        margin-left: auto;
        margin-right: auto;
        margin-bottom: 20px;
    }

    label {
        color: white;
        font-size: small;
        font-family: 'Maiandra GD';
    }

</style>


<h1>Device Management</h1>

<h2>Add new device</h2>

<form action="~/Home/SaveDevice" method="post">
    <label>Device name: </label>
    <input name="name" id="name" />
    <input type="submit" value="Save device" />
</form>

@*<button name="All devices" type="button" onclick="location.href='@Url.Action("AllDevices", "Home")'" />*@


@if (ViewBag.Devices != null)
{
    <div>
        @if (ViewBag.Result != null)
        {
            @if (ViewBag.Result.Error == true)
            {
                <div class="error-msg" id="error-msg">
                    <p style="color: red">@ViewBag.Result.Message</p>
                </div>
            }
            else
            {
                <div class="success-msg" id="success-msg">
                    <p style="color: green">@ViewBag.Result.Message</p>
                </div>
            }
        }

        <h5>Devices:</h5>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Is on remont</th>
                    <th>Send device to remont</th>
                    <th>&nbsp;</th>
                    <th>&nbsp;</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in ViewBag.Devices)
                {
                    <tr>
                        <td>
                            @item.Id
                        </td>
                        <td>
                            @item.Name
                        </td>
                        <td>
                            @item.IsOnRemont
                        </td>
                        <td>
                            @if (item.IsOnRemont == false)
                            {                            
                            <form action="~/Home/AllRemonts" method="post">
                                <input type="hidden" value="@UInt32.Parse(item.Id)" name="id" id="id" />
                                <input type="submit" value="Remont">
                            </form>
                            }
                            else
                            {
                                <p>Device is on remont</p>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <p>There is no devices.</p>
}

<form action="~/Home/ShowAllHistoryRemonts" method="post">
    <input type="submit" value="History Remonts">
</form>

<form action="~/Home/ShowRemonts" method="get">
    <input type="submit" value="All Remonts">
</form>